name: Read Data Function
description: Read NASA machine data from local system

outputs:
- {name: Data, type: LocalPath, description: 'Path where data will be stored.'}

implementation:
  container:
    image: us-central2-docker.pkg.dev/clever-tube-317207/quickstart-docker-repo2/quickstart-image:tag1
    command: [
      python, extract_data_rul.py,

      --data,
      {outputPath: Data},
    ]
